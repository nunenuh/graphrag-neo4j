# Frontend environment variables (Vite)
# Copy to .env.local and fill in values

# Backend API URL (empty = use Vite proxy in dev)
VITE_API_URL=

# API key for X-API-Key header (must match backend APP_X_API_KEY)
VITE_API_KEY=changeme-in-production
